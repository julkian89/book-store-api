version: '3.8'

services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: bookStore-api
    ports:
      - "8080:8080"
    networks:
      - bookStore-network

networks:
  book-store-network:
    driver: bridge